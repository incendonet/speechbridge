CREATE GROUP sbusers LOGIN CREATEUSER CREATEDB;
CREATE USER sbuser1 IN GROUP sbusers ENCRYPTED PASSWORD '${POSTGRES_SBPASSWORD}';
CREATE DATABASE dSpeechBridge WITH OWNER = sbusers ENCODING = 'UNICODE';
